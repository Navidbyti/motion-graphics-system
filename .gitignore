# Secrets — never commit
.env
.env.*
!.env.example
API.txt
*api-key*
*apikey*

# Dependencies
node_modules/

# Build + render output
engine/out/
exports/
.thumbnails/
app/dist/
release/
dist/

# Rendered media — these are outputs, not source
*.mov
*.mp4
*.webm

# OS / editor noise
Thumbs.db
.DS_Store
app/build/
